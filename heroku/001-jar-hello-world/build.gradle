plugins {
    id 'java'
    id 'application'
}

mainClassName = 'Main'

repositories {
    jcenter()
}

dependencies {
  compile 'org.json:json:20180813'
    }
    
task stage(dependsOn: ['clean', 'installDist'])
