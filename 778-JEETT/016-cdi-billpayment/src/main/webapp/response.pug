<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
html(xmlns="http://www.w3.org/1999/xhtml", 
     xmlns:h="http://xmlns.jcp.org/jsf/html",
     xmlns:ui="http://xmlns.jcp.org/jsf/facelets",
     xmlns:f="http://xmlns.jcp.org/jsf/core")

  
  ui:composition(template="/template.xhtml")
    ui:define("name"="title") Bill Payment: Result
    ui:define("name"="head") Bill Payment: Result
    ui:define("name"="content") 
      h:form
        h3 
          | Amount Paid with
          h:outputText#debit(value="Debit Card: ", rendered=`#{paymentBean.paymentOption eq 'DEBIT'}`)/
          h:outputText#credit(value="Credit Card: ", rendered=`#{paymentBean.paymentOption eq 'CREDIT'}`)/
          h:outputText#result(value=`#{paymentBean.value}`)
            f:convertNumber(type="currency")
        h:commandButton#back( value="back", action="index")/
          

          