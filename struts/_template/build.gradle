apply plugin: 'java'
apply plugin: 'war'

repositories {
    jcenter()
}

ext.STRUTS_VERSION = '2.5.16';

dependencies {
  // for jsp errors  
  providedCompile 'javax.servlet:javax.servlet-api:3.1.0'
  
  
  compile 'org.apache.struts:struts2-core:'+STRUTS_VERSION
  compile 'org.apache.logging.log4j:log4j-core:2.10.0'
   // providedCompile 'javax.enterprise:cdi-api:1.2'
   //  compile 'org.apache.struts:struts2-cdi-plugin:'+STRUTS_VERSION
   
   // compile 'org.apache.struts:struts2-json-plugin:'+STRUTS_VERSION
  
  // compile 'org.apache.struts:struts2-bean-validation-plugin:'+STRUTS_VERSION
  // compile 'org.hibernate.validator:hibernate-validator:6.0.11.Final'
}

war {
  archiveName = 'page.war'
  destinationDir = file(System.env.JETTY_BASE+'/webapps/')
}




